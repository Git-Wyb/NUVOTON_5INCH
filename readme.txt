git@github.com:Git-Wyb/NUVOTON_5INCH.git

1、2024.04.03上传，软件陈工TBNUVA01源代码(时间点2023/3/1下午5:52:10)

2、G6G7修正的原因是，G6G7要消去干净，只是当时显示不消去。内部消去但不显示
cyw317164627 on 2023/3/2下午1:58:13

3、音量不变不播，规避音量或文件播错
cyw317164627 on 2023/5/23上午9:47:51

2024.04.16  TBNUVA05_DEMO12，数组filename_new访问越界，现长度增加一个字节，
解决问题：抽出FUNCTION_MEMORY文件时，有时出现文件名后缀.CSV$

2024.04.18  TBNUVA05_DEMO13 == TBNUVA05 发给客人
1、问题：U盘写入图像文件时，文件夹名称前缀符合都会被写入，
修正：扫描到一个图像文件夹符合写完就break，则不再继续扫描图像文件夹。
2、解决：E1指定清除一个领域时，之前W指令写入的其他领域数据也会被清除，
修正：执行E1指令之前将其他领域数据存入缓存，防止执行E1后丢失。

2024.05.30  TBNUVA06_20240530
1、添加SPI FLASH备份。(如果CHECKSUM不对则使用备份数据，如果CHECKSUM正确，然后对比数据正确之后才使用。)
2、DATACLASS1,2数据抽出错误。A06修正(新唐内部数据线是16bit的，32bit数据拷贝时使用memcpy,不使用for循环赋值运算)
3、D命令1，2层超出张数不显示不报错，A06修正，需要报错。
4、Q0・Q1以外(Q2)的时候、没有输出指令受信错误没有报错A01，重新上电受信报错，但是受信10次之后第11次就不报错了，没有应答回复。A06修正(赋值时，变量的类型不同，导致数据错误,COMMAND_IN由8位改为16位).
5、声音连续播放噪声。A06修正(连续声音播放时，由于功放控制延迟导致声音被切掉一部分).
6、提交给客人的NAND映射表浪费了1块。A06修正(I命令时，分配LOG区域最后一块1965时判定为报错，变更为可以设定，改为if((tp_field.addr + tp_field.space)>0x9620000))。
7、TYPE_PRODUCT = PORDUCT_5INCH;区分5寸/7寸。

2024.06.12  TBNUVA06  checksum：C190  下午发给了客人。
编译器版本改为了V5.06 update 5(build 528)生成的A06版本，与软件陈工的编译器版本一样。

2024.06.13  TB240501  CHECKSUM：bd47
7inch软件。

2025.04.10  7inch,对应QA011-1项：SW2写完瑞萨hex之后重启瑞萨mcu。

2025.04.11  7inch,对应QA016项：修正，关电之后不要出现异常音。










